title: "PostgreSQL"
description: "Installs PostgreSQL, a powerful, open-source object-relational database system."
commands:
  - "sudo apt update"
  - "sudo apt install -y postgresql postgresql-contrib"
  - "sudo systemctl enable postgresql"
  - "sudo systemctl start postgresql"
check_command: "systemctl is-active --quiet postgresql"
